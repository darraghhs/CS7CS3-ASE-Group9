### Simple script to query database and record latency, printing average and maximum latency

### Steps to run app
1. Run `pip3 install -r requirements.txt`
2. Run `export GOOGLE_APPLICATION_CREDENTIALS="<absolute path to firebase admin creds .json>"`
3. Run `python3 latency.py`

