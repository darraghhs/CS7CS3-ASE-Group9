name: Merge Tests

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install pytest
        cd thin_slice/exapmle_route_app
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    
    - name: Run syntax tests
      run: |
        pytest thin_slice/tests/ -v